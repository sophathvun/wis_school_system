import { renderPagination, renderPageInfo } from "./helpers/pagination.js";
import { showSuccess, showConfirm, showError } from "./helpers/sweet-alert2.js";
const csrf = document.querySelector('meta[name="csrf-token"]')?.content;
const table = document.getElementById("occupationsTable"); const search = document.getElementById("occupations-search"); const perPage = document.getElementById("occupations-per-page"); const form = document.getElementById("occupationForm"); const modal = new bootstrap.Modal(document.getElementById("occupationModal")); let rows = [];
const escapeHtml = (value = "") => String(value).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
const reset = () => { form.reset(); document.getElementById("occupation_id").value = ""; document.getElementById("occupation_status").value = "1"; document.getElementById("occupationModalTitle").textContent = "New Occupation"; form.querySelector("[data-alert]")?.classList.add("d-none"); };
const edit = (id) => { const item = rows.find((row) => row.id === id); if (!item) return; document.getElementById("occupation_id").value = item.id; document.getElementById("occupation_name_en").value = item.occupation_name_en || ""; document.getElementById("occupation_name_kh").value = item.occupation_name_kh || ""; document.getElementById("occupation_status").value = String(item.status ?? 1); document.getElementById("occupationModalTitle").textContent = "Edit Occupation"; modal.show(); };
const remove = async (id) => { if (!(await showConfirm("Delete Occupation", "Deactivate assigned occupations instead of deleting them.", "Delete", "Cancel")).isConfirmed) return; const response = await fetch(`/occupations/${id}`, { method: "DELETE", headers: { Accept: "application/json", "X-CSRF-TOKEN": csrf } }); const result = await response.json(); if (!response.ok) return showError("Unable to delete occupation", result.message || "Unable to delete occupation."); showSuccess("Deleted", result.message); fetchRows(); };
const fetchRows = async (page = 1, size = null) => { const pageSize = size ?? parseInt(perPage.value, 10); const response = await fetch(`/occupations/fetch?page=${page}&perPage=${pageSize}&search=${encodeURIComponent(search.value)}`, { headers: { Accept: "application/json" } }); const result = await response.json(); if (!response.ok) throw new Error(result.message || "Unable to load occupations."); rows = result.data || []; table.innerHTML = rows.length ? rows.map((item) => `<tr><td>${escapeHtml(item.occupation_name_en)}</td><td class="school-profile-khmer">${escapeHtml(item.occupation_name_kh || "-")}</td><td>${item.status ? "<span class='badge bg-success-lt'>Active</span>" : "<span class='badge bg-danger-lt'>Inactive</span>"}</td><td class="text-center"><button class="btn btn-primary btn-sm" data-edit="${item.id}">Edit</button> <button class="btn btn-danger btn-sm" data-delete="${item.id}">Delete</button></td></tr>`).join("") : `<tr><td colspan="4" class="text-center">No occupations found.</td></tr>`; table.querySelectorAll("[data-edit]").forEach((button) => button.addEventListener("click", () => edit(Number(button.dataset.edit)))); table.querySelectorAll("[data-delete]").forEach((button) => button.addEventListener("click", () => remove(Number(button.dataset.delete)))); renderPagination(result, "occupations-pagination-container", "occupations-per-page", fetchRows); renderPageInfo(result); };
document.getElementById("newOccupation")?.addEventListener("click", () => { reset(); modal.show(); });
form?.addEventListener("submit", async (event) => { event.preventDefault(); const response = await fetch("/occupations/save", { method: "POST", headers: { Accept: "application/json", "X-CSRF-TOKEN": csrf }, body: new FormData(form) }); const result = await response.json(); if (response.status === 422) { const alert = form.querySelector("[data-alert]"); alert.textContent = result.message || Object.values(result.errors || {})[0]?.[0] || "Please correct the form."; alert.classList.remove("d-none"); return; } if (!response.ok) return showError("Unable to save occupation", result.message || "Unable to save occupation."); modal.hide(); showSuccess("Saved", result.message); fetchRows(); });
perPage?.addEventListener("change", () => fetchRows(1, parseInt(perPage.value, 10))); search?.addEventListener("input", () => fetchRows(1, parseInt(perPage.value, 10))); fetchRows().catch((error) => showError("Unable to load occupations", error.message));
