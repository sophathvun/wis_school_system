@extends('layouts.app')
@section('title','Notification Management')
@section('page-header')<div class="container-fluid"><div class="row g-2 align-items-center"><div class="col"><div class="page-pretitle">Administrator</div><h2 class="page-title">Notification Management</h2></div><div class="col-auto"><a class="btn btn-primary" href="{{ route('notifications.send') }}"><i class="ti ti-send me-1"></i> Send Notification</a></div></div></div>@endsection
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="card"><div class="card-header"><h3 class="card-title">All Notifications</h3></div><form method="GET"><div class="card-body border-bottom py-3"><div class="row g-2 align-items-center justify-content-between"><div class="col-auto text-secondary">Show <div class="mx-2 d-inline-block"><select class="form-control form-control-sm" name="per_page" onchange="this.form.submit()"><option value="10" @selected(request('per_page',10)==10)>10 / page</option><option value="25" @selected(request('per_page')==25)>25 / page</option><option value="50" @selected(request('per_page')==50)>50 / page</option><option value="100" @selected(request('per_page')==100)>100 / page</option></select></div> entries</div><div class="col-auto"><div class="input-icon"><span class="input-icon-addon"><i class="ti ti-search icon"></i></span><input class="form-control form-control-sm" name="search" value="{{ request('search') }}" placeholder="Search notifications"></div></div></div></div></form><div class="table-responsive"><table class="table card-table table-vcenter"><thead><tr><th>Recipient</th><th>Title</th><th>Type</th><th>Status</th><th>Sent</th><th class="text-center">Actions</th></tr></thead><tbody>@forelse($notifications as $notification)<tr><td>{{ $notification->user?->name }}<div class="text-secondary small">{{ $notification->user?->email }}</div></td><td>{{ $notification->title }}<div class="text-secondary small text-truncate" style="max-width:280px">{{ $notification->message }}</div></td><td>{{ ucfirst($notification->type) }}</td><td>{{ $notification->read_at ? 'Read' : 'Unread' }}</td><td>{{ $notification->created_at?->diffForHumans() }}</td><td class="text-center"><a class="btn btn-sm btn-outline-primary" href="{{ route('notifications.manage',['edit'=>$notification->id,'per_page'=>request('per_page',10),'search'=>request('search')]) }}"><i class="ti ti-edit"></i></a><form class="d-inline" method="POST" action="{{ route('notifications.delete',$notification) }}" onsubmit="return confirm('Delete this notification?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="ti ti-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-4">No notifications found.</td></tr>@endforelse</tbody></table></div><div class="card-footer"><div class="d-flex justify-content-center">@include('partials.admin-pagination',['paginator'=>$notifications])</div></div></div>
<div class="modal modal-blur fade" id="notificationEditModal" tabindex="-1"><div class="modal-dialog modal-dialog-centered"><div class="modal-content"><form method="POST" action="{{ $editNotification ? route('notifications.update',$editNotification) : '#' }}">@csrf<div class="modal-header"><h5 class="modal-title">Edit Notification</h5><a class="btn-close" href="{{ route('notifications.manage',['per_page'=>request('per_page',10),'search'=>request('search')]) }}"></a></div><div class="modal-body"><label class="form-label">Type</label><input class="form-control mb-3" name="type" value="{{ old('type',$editNotification?->type) }}" required><label class="form-label">Title</label><input class="form-control mb-3" name="title" value="{{ old('title',$editNotification?->title) }}" required><label class="form-label">Message</label><textarea class="form-control mb-3" name="message" rows="5" required>{{ old('message',$editNotification?->message) }}</textarea><label class="form-label">Action URL</label><input class="form-control" type="url" name="action_url" value="{{ old('action_url',$editNotification?->action_url) }}"></div><div class="modal-footer"><a class="btn me-auto" href="{{ route('notifications.manage',['per_page'=>request('per_page',10),'search'=>request('search')]) }}">Cancel</a><button class="btn btn-primary">Save Changes</button></div></form></div></div></div><script>document.addEventListener('DOMContentLoaded',()=>{const m=document.getElementById('notificationEditModal');@if($editNotification)new bootstrap.Modal(m).show();@endif});</script>
@endsection
